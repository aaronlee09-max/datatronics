name: Import Drive Fonts

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  import:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Google Drive font synchronization is disabled. Fonts are maintained in this repository."

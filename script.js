const fonts=[{name:"호승쌤체",category:"Handwriting",preview:"오늘도 예쁘게 기록해요",description:"폰트 파일을 연결하면 이 카드에서 바로 다운로드할 수 있습니다.",file:null},{name:"폰트 준비 중",category:"Coming soon",preview:"Aa 가나다 123",description:"다음 폰트를 순차적으로 추가할 예정입니다.",file:null}];
const grid=document.querySelector('#fontGrid'),count=document.querySelector('#count'),empty=document.querySelector('#empty'),search=document.querySelector('#search');
function render(){const q=search.value.trim().toLowerCase();const list=fonts.filter(f=>`${f.name} ${f.category} ${f.description}`.toLowerCase().includes(q));count.textContent=`${list.length} font${list.length===1?'':'s'}`;empty.hidden=list.length!==0;grid.innerHTML=list.map((f,i)=>`<article class="font-card"><div class="font-meta"><span class="font-name">${escapeHtml(f.name)}</span><span class="tag">${escapeHtml(f.category)}</span></div><div class="preview">${escapeHtml(f.preview)}</div><div class="font-info">${escapeHtml(f.description)}</div><div class="actions">${f.file?`<a class="download" href="${encodeURI(f.file)}" download>다운로드</a>`:'<button class="download" type="button" disabled style="opacity:.45">파일 준비 중</button>'}<button class="details" type="button" data-copy="${escapeAttr(f.name)}">이름 복사</button></div></article>`).join('');grid.querySelectorAll('[data-copy]').forEach(b=>b.addEventListener('click',async()=>{try{await navigator.clipboard.writeText(b.dataset.copy);const old=b.textContent;b.textContent='복사됨 ✓';setTimeout(()=>b.textContent=old,1000)}catch{}}))}
function escapeHtml(s){return s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function escapeAttr(s){return escapeHtml(s)}
search.addEventListener('input',render);document.querySelector('#themeBtn').addEventListener('click',()=>{document.body.classList.toggle('dark');localStorage.setItem('font-theme',document.body.classList.contains('dark')?'dark':'light')});if(localStorage.getItem('font-theme')==='dark')document.body.classList.add('dark');render();

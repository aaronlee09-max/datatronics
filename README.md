# iPhone Fonts

`datatronics` is now a lightweight, mobile-first font library for iPhone users.

- Responsive iPhone-first layout
- Font search
- Font preview cards
- Download-ready font slots
- Light / dark mode
- GitHub Pages compatible

Font files can be added under `fonts/` and referenced from the font data in `script.js`.
